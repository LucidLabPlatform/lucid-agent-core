"""
LUCID Agent Core — MQTT runtime agent for LUCID.

Connects to a broker, publishes online/offline status (LWT), subscribes to
component install commands, and loads components from the registry.
"""
