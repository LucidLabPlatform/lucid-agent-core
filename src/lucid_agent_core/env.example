# LUCID Agent Core — environment configuration
# Copy to .env for local dev (make setup). For systemd: agent-core.env at /home/lucid/lucid-agent-core/

# Required (service will fail to start if missing)
MQTT_HOST=mqtt.example.com
MQTT_PORT=1883
AGENT_USERNAME=agent-pi
AGENT_PASSWORD=your_secure_password

# Optional
# AGENT_HEARTBEAT: seconds between retained "online" status updates (default: 30)
AGENT_HEARTBEAT=30

# Optional: log level at startup (DEBUG, INFO, WARNING, ERROR). Can also be set via MQTT cmd/cfg/set.
# LUCID_LOG_LEVEL=INFO

# Optional: path to a local wheel for install/upgrade (skips download). Used by installer/upgrader.
# LUCID_AGENT_CORE_WHEEL=/path/to/lucid_agent_core-*.whl

# Optional: override base directory for agent files (default: /home/lucid/lucid-agent-core). Used for testing.
# LUCID_AGENT_BASE_DIR=/custom/path
