# LUCID Agent Core — Configuration
# Copy to .env: cp env.example .env
# Or set once per machine: mkdir -p ~/.config/lucid-agent-core && cp env.example ~/.config/lucid-agent-core/.env

# Deployment mode: local (Python on host)
LUCID_MODE=local

# MQTT Broker
MQTT_HOST=mqtt.example.com
MQTT_PORT=1883
AGENT_USERNAME=agent-pi
AGENT_PASSWORD=your_secure_password

# Optional
AGENT_HEARTBEAT=30
